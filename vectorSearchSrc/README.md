For searchquery.js:
1. Add interactivity to the search bar textbox to allow for reading capabilities
2. Add interactivity to the search bar button to trigger the code to read the textbox
3. Send search bar query to the vector search Python script (do not consider extra keywords in search)

For filter.js:
1. Add interactivity to the filter system (make each label in the list equal either 1 [True] or 0 [False])
2. When the button for the search bar is clicked the list will be checked, and all values that equal 1 will be added to the filter list.
3. Send filter list to the vector search Python script
4. Hyperlink filters? (check with Rowe on his request for this)

For displayresults.js: (Work with Reagan on this)
1. Get the list of documents from the vector search (list of documents may be a hash table array or an XML file)
2. Display those results onto the website